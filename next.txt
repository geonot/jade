ok, back to the initial work, here was the audit finding - 
```
COMPREHENSIVE MIR CODEGEN AUDIT REPORT
Document: Complete MIR Pipeline Analysis
Pipeline: HIR → MIR Lowering → MIR Optimization → Perceus Analysis → LLVM Codegen
Files Analyzed: mod.rs (249 lines), lower.rs (1,354 lines), opt.rs (936 lines), printer.rs (173 lines), mir_codegen.rs (2,410 lines)
Total: 5,122 lines of code

FINDINGS SUMMARY
CRITICAL ISSUES (Must Fix)
[lower.rs:1012-1035] ListComp deforestation missing— Generator pattern not used

Line: 1012-1035 (ListComp expression handling)
Issue: List comprehension is desugared to manual VecNew + loop + VecPush. However, there's no optimization or fusion pattern. The comment at line 411 mentions "vec = VecNew(); for bind in iter..end { if cond { VecPush(vec, body) } }" is implemented but not fused.
Severity: CRITICAL (pessimization, not a bug)
Fix: No code fix needed, but document why deforestation isn't applied.
[lower.rs:950-1000] Select statement variable demotion may fail silently

Lines: 950-1000 (Select expression handling)
Issue: At line 967-980, variables assigned in any arm are demoted. However, if demote_vars_to_memory fails to find a variable in var_map, it silently does nothing. If a variable is referenced before assignment, it won't be in var_map and demotion is skipped, but the later assignment still tries to store to memory.
Severity: CRITICAL (data corruption/wrong semantics)
Example: let x; select { case x { ... } } — x is uninitialized, demotion fails silently, assignment stores to uninitialized memory.
Fix: Either require all select-assigned variables to exist beforehand (enforce in type-check), or pre-initialize them in lowering.
[mir_codegen.rs:559-600] StructInit field ordering not validated

Lines: 559-600 (StructInit codegen)
Issue: Fields are inserted into struct in the order they appear in the MIR Vec<(String, ValueId)>, using field_order from comp.structs. However, if the MIR field order doesn't match the canonical struct layout, the fields are inserted in the wrong position. There's no validation that field names exist in the struct.
Severity: CRITICAL (silent field corruption)
Fix: Validate that all field names in the init exist in the canonical layout and sort them before insertion.
[mir_codegen.rs:1260-1290] VariantInit payload storage is unsafe

Lines: 1260-1290 (VariantInit codegen)
Issue: Payload bytes are stored sequentially at byte offsets (line 1279-1295). But there's no validation that offset calculations don't overflow the payload area size. If the struct has nested types or alignment requirements, the offset math is wrong.
Severity: CRITICAL (buffer overflow potential)
Fix: Calculate full payload size first, validate against storage.
[lower.rs:510-520] Lambda closure variable capture incomplete

Lines: 510-550 (Lambda RHS lowering)
Issue: At line 530-544, captured variables are collected. However, collect_expr_var_refs_block (defined at line 1226) doesn't handle all expression types:
Missing: ExprKind::Select, ExprKind::MethodCall, ExprKind::Field, ExprKind::IfExpr
These aren't recursively analyzed for variable references.
Severity: CRITICAL (lambda capture correctness)
Fix: Complete the visitor in collect_expr_var_refs_expr.
[opt.rs:73-100] Copy propagation doesn't eliminate Copy instructions

Lines: 73-100 (after copy_propagation)
Issue: After substituting all uses of Copy destinations with their sources, the Copy instructions themselves are NOT removed. This means the instructions remain dead, but only if DCE (Dead Code Elimination) runs afterward. If DCE doesn't run (e.g., OptLevel::Basic), dead copies accumulate.
Severity: HIGH (code quality, not correctness)
Fix: In copy_propagation, filter out the Copy instructions whose destinations are in the substitution map.
HIGH-SEVERITY ISSUES
[lower.rs:715-750] Match statement phi ordering

Lines: 715-750 (Switch-based match handling)
Issue: At line 722-750, when multiple arms branch to the merge block, phi entries are added out-of-order. The phi_entries vector builds a list of (value, block) tuples, but when multiple arms have different ending blocks (due to control flow splits), the mapping of blocks to phi predecessors is wrong.
Severity: HIGH (phi node generation bug)
Example: If arm1 ends in block5 and arm2 ends in block6, but they both branch to merge7, the phi at merge7 should have incoming: [block5, arm1_val], [block6, arm2_val]. The current code doesn't track which arm ends in which block after intra-arm control flow.
Fix: Keep track of the final block of each arm, not just the last statement's block.
[mir_codegen.rs:520-530] FnRef wrapper leaks memory

Lines: 520-530 (FnRef codegen)
Issue: The fn_ref_wrapper delegates to comp.fn_ref_wrapper() which isn't a real function in the existing compiler (it's not shown in the read files). This suggests the wrapper generation is incomplete. If the function doesn't exist, a wrapper is created but never stored, and the reference might be dangling.
Severity: HIGH (possible UB)
Fix: Verify that wrapper caching is implemented in the compiler's wrapper pool.
[opt.rs:360-380] Phi simplification unsoundness

Lines: 360-380 (simplify_phis)
Issue: Line 368-370: If all incoming values to a phi are identical (ignoring self-references), the phi is replaced. But unique.len() == 1 is checked AFTER filtering out self-references. A phi like v5 = phi [v5, v5, v6] would have unique = {v5, v6}, len=2, so it's not simplified. But if it was v5 = phi [v5] (self-loop), unique = {}, len=0, it's simplified to... itself? The code doesn't handle the edge case.
Severity: HIGH (phi node handling edge case)
Fix: Add explicit handling for phi nodes with no distinct incoming values.
[lower.rs:820-860] For loop: end_val repeated evaluation

Lines: 820-860 (For statement lowering, range case)
Issue: Line 828: end_val is computed once for range-for loops. But in loop increment (line 841), the loop back-edges to line 824, which re-evaluates the condition counter < end_val. If end_val requires computation (e.g., a function call lowered as Call), it's recomputed every iteration! Only constant end values are truly loop-invariant.
Severity: HIGH (performance regression)
Fix: Store end_val in a memory variable, load it in the condition.
[lower.rs:890-920] SimFor (parallel for) has same loop issues

Lines: 890-920 (SimFor statement)
Issue: Same as #10 — end_val is recomputed without Store/Load protection.
Severity: HIGH (performance regression)
Fix: Same as #10.
[mir_codegen.rs:1790-1850] store_insert doesn't propagate field types correctly

Lines: 1790-1850 (emit_store_insert)
Issue: For each field at lines 1810-1820, the code calls comp.llvm_ty(param_ty) directly. But param_ty comes from sd.fields[*] which is a stored Type. If this type was simplified or coerced during lowering, it might not match the record struct's actual field types.
Severity: HIGH (store operations silent failure)
Fix: Validate field types match the record struct definition.
MODERATE ISSUES
[opt.rs:440-470] LICM doesn't validate loop detection

Lines: 440-470 (loop_invariant_code_motion)
Issue: At line 448-460, back-edges are detected by layout order (block i → block j where j ≤ i). But this assumes blocks are topologically ordered, which isn't guaranteed after merging/DCE. A block inserted out of order could create false loop headers.
Severity: MODERATE (incorrect optimization scope)
Fix: Use proper dominance analysis instead of layout order.
[opt.rs:455-465] LICM preheader detection is incomplete

Lines: 455-465
Issue: A "preheader" is identified as the single predecessor outside the loop. But if there are multiple paths into the loop header (e.g., multiple entry points), LICM skips the loop (line 469: continue). However, LICM should still work with multiple entries as long as one is unconditional. The current code is overly strict.
Severity: MODERATE (missed optimization)
Fix: Allow multi-entry loops and hoist to all entry blocks.
[opt.rs:160-180] Store/load forwarding doesn't handle nested scopes

Lines: 160-180 (store_load_forwarding)
Issue: Forwarding is per-block (line 156: for bb in &func.blocks). If a variable is stored in block A, then block B (reachable from A) loads it, the forwarding doesn't apply. This is conservative but might miss optimization opportunities.
Severity: MODERATE (optimization gap)
Fix: Extend to flow-insensitive approach with dominance.
[lower.rs:380-410] lower_expr doesn't handle all HIR expression types

Lines: 380-410 and scattered
Issue: There are 50+ ExprKind variants (from the grep results). The lower_expr match is comprehensive, but there are catch-all patterns (e.g., line 1038: _ => { ... }). If a new ExprKind is added to HIR, it's silently lowered as a call. This could hide bugs.
Severity: MODERATE (maintainability)
Fix: Mark the match as #[must_use] or add explicit exhaustiveness check.
[mir_codegen.rs:1530-1560] Index codegen is incomplete for vectors

Lines: 1530-1560 (Index handling for arrays)
Issue: Lines 1535-1555 handle array indexing via build_gep. But for vectors (heap-allocated), the code returns void_val() (line 1558). This means all v[i] expressions on vectors return 0!
Severity: MODERATE (vectors not working)
Fix: Call jade_vec_index runtime function for vector indexing.
[mir_codegen.rs:1650-1700] ClosureCall environment unpacking has no bounds check

Lines: 1650-1700 (ClosureCall codegen)
Issue: Lines 1670-1690 extract capture values from the environment struct via struct GEP. But if the number of captures doesn't match the function's parameter count, there's a mismatch. There's no validation.
Severity: MODERATE (closure safety)
Fix: Validate capture count matches declaration.
[lower.rs:635-670] IfExpr doesn't demote variables assigned in merges

Lines: 635-670 (IfExpr handling)
Issue: IfExpr is different from Stmt::If. IfExpr lowering creates phi nodes but doesn't demote variables. If a variable is assigned inside the IfExpr's branches, it's not memory-backed, leading to SSA violation later.
Severity: MODERATE (SSA correctness)
Fix: Apply same demotion logic as Stmt::If.
[mir_codegen.rs:600-630] ArrayInit for empty arrays

Lines: 600-630
Issue: Line 603-608 handles empty arrays by returning arr_ty.const_zero(). But the const_zero value might not be a valid array (if arr_ty is a struct or opaque type). Also, the LLVM type is inferred from inst.ty (a Jade Type), which might mismatch.
Severity: MODERATE (edge case handling)
Fix: Ensure const_zero works for all array types.
[opt.rs:215-240] Constant folding for signed/unsigned operations

Lines: 215-240 (fold_binop)
Issue: fold_binop uses checked_* arithmetic (e.g., checkd_add). If overflow occurs, the operation returns None, and constant folding is skipped. But the original BinOp instruction is left as-is (with non-constant operands), which means overflow behavior at compile-time differs from runtime. For signed integers that wrap in Jade semantics, this is incorrect.
Severity: MODERATE (semantic mismatch)
Fix: Use wrapping arithmetic for BinOp::Add/Sub/Mul/Div/Mod.
[lower.rs:1120-1150] For loop: loop index variable not properly SSA-ified

Lines: 1120-1150 (Collection-for lowering)
Issue: At line 1128-1150, the loop index is stored and loaded via Store/Load. But the index is initially assigned via a Bind (missing), then assigned in each iteration. The initial value (0) should be stored at the beginning, but the code at line 1141 only stores it inside the condition block after creating the counter.
Severity: MODERATE (initialization correctness)
Fix: Ensure initial store happens before condition block.
LOW-SEVERITY ISSUES
[printer.rs:60-80] Print function has no bounds checking on parameters

Lines: 60-80 (print_function)
Issue: The printer assumes func.params has enough corresponding LLVM parameters in func.params.iter() loop. If MirCodegen declared fewer LLVM params than MIR params, the printer will crash.
Severity: LOW (debug tool, not affecting compilation)
Fix: Check length equality.
[mir_codegen.rs:310-330] declare_mir_fn doesn't check for duplicate declarations

Lines: 310-330
Issue: Line 323: self.comp.fns.insert(func.name.clone(), ...) overwrites any previous declaration. If two MIR functions have the same name, the second silently shadows the first.
Severity: LOW (unlikely scenario, no detection)
Fix: Check if name already exists and error on duplicate.
[lower.rs:530-550] Lambda body doesn't validate captured variables exist

Lines: 530-550
Issue: At line 539: if let Some(&val) = self.var_map.get(name) skips variables not in var_map. But this means captured variables that don't exist are silently dropped from the capture list. No error is reported.
Severity: LOW (type-check should have caught this)
Fix: Assert that captured variables exist.
[opt.rs:295-310] Global value numbering uses string keys inefficiently

Lines: 295-310 (gvn_key)
Issue: Line 300: format!("bin:{op:?}:{},{}", a.0, b.0) allocates a string for every instruction. For large functions, this is wasteful. A hash of (op, a, b) would be more efficient.
Severity: LOW (performance micro-optimization)
Fix: Replace string keys with a proper hash struct.
[mir_codegen.rs:2370-2400] emit_select doesn't handle default arms

Lines: 2370-2400 (emit_select)
Issue: The select codegen creates case structs for all channels (lines 2382-2410) but doesn't handle the case where select has a default arm (no timeout, default behavior). The LLVM jade_select call uses has_default = false (line 2413), which might cause incorrect behavior.
Severity: LOW (select default semantics)
Fix: Detect default arm and set has_default = true.
[lower.rs:420-440] VariantRef initialization is empty

Lines: 420-440 (VariantRef expression)
Issue: Line 439: VariantRef is lowered as VariantInit(enum_name, variant_name, *tag, vec![]) with empty payload. But if the variant has fields, this is wrong. No fields are initialized.
Severity: LOW (feature gap, enum with payloads via ref)
Fix: Document or implement proper payload initialization.
ARCHITECTURAL GAPS
[mod.rs:140-160] DynDispatch instruction is unimplemented

Lines: 140-160
Issue: InstKind::DynDispatch is defined but has no codegen implementation (mir_codegen.rs line 959 returns void_val()). Dynamic trait dispatch requires vtable lookup, which is stubbed.
Severity: HIGH (feature gap)
Fix: Implement vtable dispatch or document as not-supported.
[mod.rs:160-170] Slice instruction is unimplemented

Lines: 160-170
Issue: InstKind::Slice is defined, but codegen (mir_codegen.rs line 1523) returns void_val().
Severity: MODERATE (feature gap)
Fix: Implement slice operations.
[lower.rs:1290-1310] Coroutine/generator bodies not fully lowered

Lines: 1290-1310 (CoroutineCreate)
Issue: Line 1309 lowers the body to instructions, but doesn't create a separate coroutine frame function. The body is inlined into the caller. This is incorrect semantics — a coroutine should be a separate function that can be resumed.
Severity: CRITICAL (coroutine semantics)
Fix: Create separate __coro_{name} function and adapt lowering.
[lower.rs:1100-1120] Generator for-in loop missing proper iteration

Lines: 1100-1120 (For with generator)
Issue: Lines 1100-1115 handle Type::Generator(_). The code calls __gen_resume, __gen_done, __gen_next_val as opaque functions. But these functions are never declared or defined. They're stubs.
Severity: HIGH (generator iteration broken)
Fix: Implement generator runtime or lower differently.
MISSING OPTIMIZATIONS
[opt.rs] No array bounds checking elimination (ABC)

Issue: MIR could eliminate bounds checks for provably-safe index operations (e.g., for i in 0..len { arr[i] }). Currently, all index operations compile to runtime bounds checks.
Severity: LOW (performance)
[opt.rs] No inlining pass

Issue: Small functions like field accessors could be inlined during MIR optimization. Currently, all calls are opaque.
Severity: MODERATE (performance)
[opt.rs] No specialization pass

Issue: Polymorphic functions could be specialized per-callsite type. Not implemented.
Severity: LOW (advanced optimization)
SUMMARY TABLE
Category	Count	Severity	Status
Critical	6	Must Fix	2 fixed (variable lowering, elif chains), 4 remaining
High	6	Important	Mixed
Moderate	10	Should Fix	Several performance regressions
Low	6	Polish	Mostly edge cases
Total	28	—	—
PRIORITY REMEDIATION PLAN
IMMEDIATE (Critical Path):

Fix #2 (Select uninitialized variable demotion)
Fix #3 (StructInit field ordering validation)
Fix #4 (VariantInit payload bounds)
Fix #31 (Coroutine body lowering semantics)
SHORT TERM (High Impact):

Fix #5 (Lambda capture incompleteness)
Fix #7 (Match phi ordering)
Fix #17 (Vector indexing)
Fix #1 (Document ListComp behavior)
MEDIUM TERM (Quality):

Fix #6, #9 (Copy/Phi edge cases)
Fix #10, #11 (Loop invariants)
Fix #13, #14 (LICM correctness)
LONG TERM (Features):

Implement #29 (Dynamic dispatch)
Implement #32 (Generator iteration)
This audit supersedes the prior March 2026 report by adding 28 specific issues with line numbers, rationales, and fix strategies. The codebase has strong foundational fixes (variable demotion, elif chains, phi handling) but needs refinement in boundary conditions and advanced features.
```

and the initial prompt - 

```
Assess the MIR Codegen Implementation rigorously and completely. Identify any gaps, shortcomings, issues, problems, partial implementations, bugs, regressions or unimplemented optimizations. Work to remediate any identified items and ensure that the feature is 100% complete. Add tests and benchmarks, and validate the implementation through existing tests and benchmarks.

With regard to benchmrks, I suspect that any benchmark results in the microseconds may be code that is optimized out due to result not being used, etc. Ideally all benchmarks should do at least a second of work in order to accurately capture the speed of the respective binaries. work to remediate the suite of benchmarks to provide a more accurate performance analysis.
```
