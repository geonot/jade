{"ts": "2026-06-05T15:06:11+00:00", "note": "PARTIAL machinery already exists from prior sessions:\n- Lexer: Token::BangBang (`!!`), Token::Question (`?`), Token::Bang (`!`), Token::Dollar (`$`), Token::QuestionGt (`?>`) all tokenized (src/lexer/mod.rs:425-440, token.rs).\n- Parser: `err` keyword -> Expr::Ident(\"err\") (parse_primary.rs:9). `$` -> Expr::Placeholder (parse_primary.rs:478). Quaternary `call ? ok ! falsy !! VARIANT` handled in bind.rs:83-175 and finish_bare_handler_chain (bind.rs:394) + finish_bare_bangbang (357). dispatch.rs:348-422 handles Bang/BangBang stmt forms.\n- Typer: desugar_bang_ret (resolve.rs) makes `! E` => Result of R,E. try_wrap_err_return (stmt/dispatch.rs) handles From conversion + missing-conversion diag. auto_wrap_ok/auto_unwrap_result/result_enum_of/ok_inner_ty_pub (call/method_call.rs). tail auto-wrap (stmt/block.rs).\nGAPS to implement:\n1. `err` in `!!` arm must bind to the actual error value (currently 'Load of undefined variable err'). The `!!` arm desugars to a match Err(e) arm; bind `err` to that payload. Current finish_bare_handler_chain only supports `!! VARIANT` (propagate that variant) not `!! expr-using-err`.\n2. `$` (Placeholder) in `?` arm must bind to unwrapped Ok value.\n3. Implicit propagation: bare `foo()` unhandled fallible => `? $ !! err`.\n4. `err NotFound` raise currently relies on `! E` desugar; verify standalone (non-Result-return inferred) path.\nTest snippet /tmp/new1.jn: `read(true) ? log($) !! log(err)` should print 42 then NotFound-ish.\nKEY DECISION from user: hard-migrate off prefix-! raise and `! sentinel`. `err X` is the only raise. `?>` to be REMOVED."}
