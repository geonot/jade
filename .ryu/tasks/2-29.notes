{"ts": "2026-06-10T10:35:31+00:00", "note": "Review P0-10 RE-VERIFIED 2026-06-10: fixed for the keyword-shadowing class. `ch.send(1)` parses and runs (send returns bool per task 2-8). `ch.close()` intentionally rejected with clean targeted diagnostic 'channel close is the statement `close {ch}`' \u2014 that is the designed surface, not a parser hole. Soft-keyword coverage already pinned by alpha_audit_soft_keyword_idents/_nouns (from, to, by, default, end, query...). Remediation = add test pinning `.send()` method-position parse + the close() diagnostic."}
