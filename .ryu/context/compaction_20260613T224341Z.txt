TASK: Implement structured concurrency in Jinn (jinnc, Rust compiler + C runtime). Working dir /home/rome/Glitch/software/jade. Deleted tasks 2-11,2-12. Completed: 2-9 (join), 2-7 (stop-and-drain), 2-10 (yield injection), 2-6-1 (together surface), 2-6-2 (runtime scope layer), 2-6-3 (together happy-path lowering). REMAINING: 2-6-4 (cancellation: stop scope + cooperative unwind), 2-6-5 (error propagation + scope error-union + escape check + docs). After those, parent 2-6 auto-completes.

CURRENT STATE — all builds clean (cargo build --release, zero warnings). Tests: tests/concurrency_shutdown.rs 14 pass. Full suite passes EXCEPT pre-existing column_max/min/sum failures in tests/integration.rs (fail on clean tree too — NOT my regression, ignore them).

KEY IMPLEMENTATION FACTS (also in memory: task_2-6-3_impl, concurrency_impl_arch, join_keyword_contextual, task_2-10_impl):
- join: `join w` contextual keyword (parser/stmt/dispatch.rs `_ => Ident` arm, guard `!matches!(peek_at(1),LParen|Dot)`). Stmt::Join(Expr,Span). NOT a lexer token (collides with std *join fns). Runtime jinn_join_t latch in actor.c, mailbox field 3 = join slot, jinn_actor_join/jinn_join_signal.
- together: Token::Together keyword. ast/hir::Stmt::Together(Option<Symbol>, Block, Span). parse_together in parser/stmt/store.rs.
- RUNTIME runtime/scope.c (in build.rs after actor.c): jinn_scope_t {atomic live_children, atomic cancelled, atomic lock, jinn_coro_t *parent, jinn_scope_t *prev, jinn_coro_t *children[64], int child_count, void *actors[64], int actor_count}. _Thread_local tl_scope. Funcs: jinn_scope_create(push), current/set_current, register_child(child->scope=cur,live++,inherit cancel,append children[]), add_actor, child_done(live--,wake parent), cancel(idempotent,mark children cancelled,close actor mailboxes via jinn_actor_stop), check_cancelled(reads tl_worker->current->cancelled), stop_actors(graceful close), join(park parent until live==0 then pop+free), actor_spawn_scoped(coro,mb)(if tl_scope: register_child+add_actor else set_daemon). jinn_coro_t in jinn_rt.h gained `void *scope` + `_Atomic(int32_t) cancelled` (init in coro.c).
- sched.c worker loop: sets jinn_scope_set_current(c->scope) before running coro, clears after; DESTROY path calls jinn_scope_child_done(c->scope).
- MIR: Lowerer.scope_stack: Vec<ValueId> (ctx.rs). lower_together (concurrency.rs effects route): emit __scope_create -> push -> lower_block_stmts -> pop -> __scope_stop_actors -> __scope_join. Anon dispatch (name.starts_with("__anon")) inside scope: captures = collect_var_refs_block(super::closures, now pub(super)) ∩ var_types, sorted, read_var'd; lower_scope_task(name,body,captures,span); emit __scope_spawn_<name>([scope, ...caps]). MIR Function gained `scheduler_task: bool` (defaults in ctx.rs + verify.rs).
- Typer concur.rs lower_expr_dispatch_block: anon renamed to unique __anon{next_id} (Symbol), keeps __anon prefix for detection.
- codegen: cur_fn_is_scheduler_task field (mod.rs, support/module.rs init). mir_codegen/mod.rs Return: `if cur_fn_is_coroutine && !cur_fn_is_scheduler_task` emits gen-suspend, else normal build_return (scheduler tasks must NOT gen-suspend → null caller_ctx segfault). emit_scope_spawn in magic.rs (builds gen struct GEN_SIZE+caps*8, stores caps, jinn_coro_create, jinn_scope_register_child, jinn_sched_spawn). magic.rs handles __scope_create/__scope_join/__scope_stop_actors/__scope_spawn_<name>. compile_spawn_with_init_vals (actors.rs) replaced set_daemon with jinn_actor_spawn_scoped(coro,mb_ptr_v).
- All scope runtime fns declared in src/codegen/support/runtime.rs.
- uses_concurrency (support/runtime.rs) scan_block recurses Together/Transaction/Defer, scan_stmt includes Together/Join (else runtime undeclared → ICE).
- @no_yield FnAttrs opt-out (ast.rs, parser/decl/fn.rs). inject_yields MIR pass (src/mir/opt/yield_passes.rs) at back-edges in is_coroutine||__actor_ fns, runs every opt level.

DESIGN: docs/structured-concurrency.md (full spec, still marked "design — not implemented"; 2-6-5 flips it). docs/concurrency.md updated for join/stop-drain/yield.

NEXT (2-6-4 cancellation): (a) `stop <scope>` — scope name binding. Currently scope name (Option<Symbol>) is parsed but NOT bound to a usable value; `stop name` would need name->scope ValueId. Need to bind scope name in lower_together to the __scope_create result and make Stmt::Stop dispatch to jinn_scope_cancel when target is a scope (vs jinn_chan_close for actor). Distinguish via type — may need a Type::Scope or track scope-named vars. (b) Park loops in runtime/channel.c (jinn_chan_recv/send) + jinn_sched_yield + injected __sched_yield must check coro->cancelled and return unwind status. (c) Cancelled unwind = early-return running defers+Perceus drops. If full unwind-codegen too invasive, do runtime park-point flagging + bounded best-effort + document deferral. Conformance targets: stop_scope_cancels_without_drain, defer_runs_on_cancellation. jinn_scope_cancel + check_cancelled already exist in runtime. Use test harness compile_and_run/expect in tests/concurrency_shutdown.rs.

WORKFLOW: use effort begin/end per subtask, task_complete when done, save after. Tests via `cargo test --release --test concurrency_shutdown`. Quick manual: write /tmp/*.jn, ./target/release/jinnc x.jn -o out && out.