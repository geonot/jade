# Sim for benchmark
# Spawns N independent coroutines via sim for

*fib(n as i64) -> i64
    if n < 2
        return n
    return fib(n - 1) + fib(n - 2)

*main
    total is 0
    sim for i in 1000
        k is 28 + total mod 5
        total is total + fib(k)
    log(total)
