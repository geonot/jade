# Sim for benchmark
# Spawns N independent coroutines via sim for

*fib(n as i64) returns i64
    if n < 2
        return n
    return fib(n - 1) + fib(n - 2)

*main
    sim for i in 1000
        fib(28 + i mod 5)
    log(0)
