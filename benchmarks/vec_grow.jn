*main
    v is vec()
    for i in 400000000
        v.push(i)
    n is v.length
    for i in 0 to n
        v.set(i, v.get(i) xor (i + 1))
    total is 0
    for i in 0 to n
        total is total + v.get(i)
    log(total)
    0
