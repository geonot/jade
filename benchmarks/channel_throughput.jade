# Channel throughput benchmark
# Send/recv 1M i64 values through a buffered channel

*main
    ch is channel of i64(1024)
    n is 1000000
    for i in 0 to n
        send ch, i
        val is receive ch
    log(0)
