
*fib(n as i64) returns i64
    if n < 2
        n
    else
        fib(n - 1) + fib(n - 2)

actor Worker
    sum
    @work n
        sum is sum + fib(n)

*main
    n is 64
    w is spawn Worker
    for i in 0 to n
        w.work(35)
    log(0)
