*sum_arr(a0: i64, a1: i64, a2: i64, a3: i64, a4: i64) -> i64
    a0 + a1 + a2 + a3 + a4

*main() -> i32
    total is 0
    i is 0
    while i < 10000000
        arr is [i, i + 1, i + 2, i + 3, i + 4]
        total is total + arr[0] + arr[1] + arr[2] + arr[3] + arr[4]
        i is i + 1
    log(total)
    0
