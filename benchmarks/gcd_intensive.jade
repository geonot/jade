*gcd(a, b)
    while b neq 0
        t is b
        b is a mod b
        a is t
    return a

*main
    sum is 0
    for i in 1 to 30000
        j is i + 1
        while j < 30000
            sum is sum + gcd(i, j)
            j is j + 10
    log(sum)
