store items
    name as String
    val as i64

*main
    insert items 'aaa', 100
    insert items 'bbb', 200
    insert items 'ccc', 300

    n is count items
    log n

    match items where val > 0
        Ok(x) ?
            log x.val
            log x.name
        Err(e) ? log 'miss'
