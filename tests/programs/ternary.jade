*abs(x)
    x >= 0 ? x ! -x

*max(a, b)
    a > b ? a ! b

*clamp(v, lo, hi)
    v < lo ? lo ! v > hi ? hi ! v

*main
    log(abs(-42))
    log(abs(42))
    log(max(10, 20))
    log(max(30, 5))
    log(clamp(50, 0, 100))
    log(clamp(-5, 0, 100))
    log(clamp(200, 0, 100))
