store scores
    value as i64

*main
    insert scores 10
    insert scores 20
    insert scores 30

    n is count scores
    log n

    match scores where value > 15
        Ok(big) ? log big.value
        Err(e) ? log 'miss'
