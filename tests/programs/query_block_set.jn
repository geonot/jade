store users
    name as String
    age as i64

*main
    insert users 'Alice', 30
    insert users 'Bob', 25

    users query
        where name equals 'Bob'
        set age is 99

    match users where name equals 'Bob'
        Ok(r) ? log r.age
        Err(e) ? log 'miss'
