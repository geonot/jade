# Hailstone (Collatz) step count.
*steps(n as i64) returns i64
    c is 0
    while n neq 1
        if n mod 2 equals 0
            n is n / 2
        else
            n is 3 * n + 1
        c is c + 1
    c

*main
    log steps(1)
    log steps(6)
    log steps(27)
