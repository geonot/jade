# LUT-based combinational logic: 4-input LUT.
*lut(table as Vec of i64, a as i64, b as i64, c as i64, d as i64) returns i64
    idx is a + b * 2 + c * 4 + d * 8
    table.get(idx)

*main
    t is vec()
    i is 0
    while i < 16
        t.push(i ^ 7)
        i is i + 1
    log lut(t, 1, 0, 1, 1)
