# HFT: bid-ask imbalance momentum signal.
*imb(bid as i64, ask as i64) returns i64
    if bid + ask equals 0
        return 0
    (bid - ask) * 1000 / (bid + ask)

*main
    log imb(500, 300)
    log imb(100, 900)
    log imb(0, 0)
