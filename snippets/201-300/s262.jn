# Even parity bit generator over byte.
*parity(x as i64) returns i64
    p is 0
    bit is 0
    while bit < 8
        p is p ^ ((x / (1 << bit)) % 2)
        bit is bit + 1
    p

*main
    log parity(0xF0)
    log parity(0x07)
    log parity(0xFF)
