# Timer interrupt schedule: count callbacks fired in window.
*ticks_in(period_us as i64, window_us as i64) returns i64
    window_us / period_us

*main
    log ticks_in(1000, 1000000)
    log ticks_in(50, 10000)
