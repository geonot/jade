# Raft term/vote tally: majority decision.
*has_majority(votes as i64, total as i64) returns i64
    if votes * 2 > total
        return 1
    0

*main
    log has_majority(3, 5)
    log has_majority(2, 5)
    log has_majority(4, 7)
