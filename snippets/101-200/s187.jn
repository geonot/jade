# Find max value & index in a vec.
*main
    a is vec(7, 1, 9, 3, 11, 5, 8)
    bi is 0
    bv is a.get(0)
    i is 1
    while i < a.length
        if a.get(i) > bv
            bv is a.get(i)
            bi is i
        i is i + 1
    log bv
    log bi
