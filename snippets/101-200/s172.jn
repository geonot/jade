*main
    m is vec(vec(1, 2, 3), vec(4, 5, 6), vec(7, 8, 9))
    t is vec(vec(0, 0, 0), vec(0, 0, 0), vec(0, 0, 0))
    i is 0
    while i < 3
        row is copy t.get(i)
        j is 0
        while j < 3
            row.set(j, m.get(j).get(i))
            j is j + 1
        t.set(i, row)
        i is i + 1
    for row in t
        for x in row
            log x
