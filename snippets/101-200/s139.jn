# Reverse a string by iterating bytes.
*reverse_str(s as String) returns String
    out is ""
    i is s.length - 1
    while i >= 0
        out is out + s.slice(i, i + 1)
        i is i - 1
    out

*main
    log reverse_str("jinn")
    log reverse_str("hello world")
