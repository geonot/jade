# Reverse a vec in place.
*rev(v)
    i is 0
    j is v.length - 1
    while i < j
        t is v.get(i)
        v.set(i, v.get(j))
        v.set(j, t)
        i is i + 1
        j is j - 1

*main
    a is vec(1, 2, 3, 4, 5)
    rev(a)
    for x in a
        log x
