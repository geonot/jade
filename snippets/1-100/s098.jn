# Build a 3x3 grid as Vec of Vec, sum it.
*main
    rows is vec(vec(1, 2, 3), vec(4, 5, 6), vec(7, 8, 9))
    total is 0
    for r in rows
        for v in r
            total is total + v
    log total
